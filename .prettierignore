node_modules
coverage
dist
*.min.js
package-lock.json
bun.lock
.eslintcache

# Verbatim lychee output kept as a parser test fixture: prettier rewrites the
# `*` bullets lychee emits into `-`, which would make the fixture stop
# resembling the reports scripts/check-web-archive.mjs has to parse.
scripts/fixtures/
